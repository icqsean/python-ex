SET Excel_File_Path TO $'''D:\\ExcelSQL\\ch12\\選課資料.xlsx'''
Database.Connect ConnectionString: $'''Provider=Microsoft.ACE.OLEDB.12.0;Data Source=%Excel_File_Path%;Extended Properties=\"Excel 12.0 Xml;HDR=YES\";''' Connection=> SQLConnection
Database.ExecuteSqlStatement.Execute Connection: SQLConnection Statement: $'''SELECT 學號, COUNT(*) AS 修課數
FROM [班級$]
GROUP BY 學號
HAVING COUNT(*) > 2;
''' Timeout: 30 Result=> QueryResult
Database.ExecuteSqlStatement.Execute Connection: SQLConnection Statement: $'''SELECT 課程編號, COUNT(學號) AS 學生數
FROM [班級$]
WHERE 教授編號 = \'I003\'
GROUP BY 課程編號
HAVING COUNT(學號) >= 2;
''' Timeout: 30 Result=> QueryResult2
Database.Close Connection: SQLConnection
